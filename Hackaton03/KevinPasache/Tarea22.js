function sumar(num) {
    let result;
    result = num * (num + 1 ) / 2
    return result  
}

// Llamar la función con un parámetro


function sumarNumero (num) {
    let result = 0;
    let i = 1;
    while ( i <= num) {
        result = result + i;
        i++
    }
    return result;
}

// Llamar función con un parámetro