const dotenv= require('dotenv');


dotenv.config();

const Server = require('./server/server.js');

const server = new Server();

server.listen();


const dotenv= require('dotenv');


dotenv.config();

const Server = require('./server/server.js');

const server = new Server();

server.listen();



